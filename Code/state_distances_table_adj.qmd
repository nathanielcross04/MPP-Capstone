---
title: "State Distances"
format:
  html:
    embed-resources: true
    page-layout: full
    echo: false
    warning: false
    message: false
execute:
  eval: true
knitr:
  opts_knit:
    root.dir: "C:/Users/ndmcr/Desktop/MPP Capstone"
---

```{r setup}
library(dplyr)
library(jsonlite)

df <- read.csv(
  "https://raw.githubusercontent.com/nathanielcross04/MPP-Capstone/refs/heads/main/Data/Final%20data/state_distances%20(raw%20and%20deltas).csv",
  check.names = FALSE
)

YEARS       <- 2000:2019
DELTA_YEARS <- 2001:2019

# ══════════════════════════════════════════════════════════════════════════════
# CUSTOM ANNOTATIONS
# Key: "StateName__YYYY"  (two underscores)
# Notes are shared across all tables for a given state+year cell.
# EDIT: add, remove, or change any note below.
# ══════════════════════════════════════════════════════════════════════════════
annotations <- list(
  # ── GLOBAL / NATIONWIDE SHOCKS ─────────────────────────────────────────────
  "District of Columbia__2001" = "DC surged to the most distinctive policy profile in the nation in 2001, driven by post-9/11 federal security mandates that reshaped local enforcement in ways no other state matched.",
  "Texas__2001"                = "Texas saw a sharp divergence in 2001. Post-9/11 border-security pressures, combined with rapid Hispanic population growth, pushed the state's immigration policy mix far from the national center.",
  "Michigan__2001"             = "Michigan's distance jumped markedly in 2001. Post-9/11 security measures, including heightened scrutiny of Arab-American communities in the Detroit metro area, pushed the state's profile away from the national norm.",

  # ── WAVE OF ENFORCEMENT STATES 2003–2005 ──────────────────────────────────
  "Alabama__2003"              = "Alabama's distance climbed sharply in 2003 after it became one of the first states to sign a 287(g) Task Force agreement (AL Dept. of Public Safety), authorising local officers to perform immigration enforcement.",
  "Oklahoma__2003"             = "Oklahoma jumped 27 ranks in 2003, the largest single-year rank change in the dataset, as it began building a comprehensive enforcement portfolio including early 287(g) agreements ahead of its later omnibus bill.",
  "Arizona__2005"              = "Arizona's biggest single-year jump — 30 ranks — came in 2005 as the state ramped up enforcement ahead of its 2010 SB1070 omnibus bill, adding 287(g) agreements and pushing E-Verify adoption far above the national average.",
  "New Mexico__2005"           = "New Mexico moved sharply away from the centroid in 2005, partly reflecting its 2003 drivers-license law for undocumented immigrants — one of the earliest in the nation — which distinguished it from neighbouring states.",

  # ── HURRICANE KATRINA DISPLACEMENT ────────────────────────────────────────
  "Louisiana__2007"            = "Louisiana's cluster distance spiked in 2007, the largest single-year own-cluster jump outside the 9/11 shock years. Post-Katrina demographic upheaval and federal rebuilding contracts created labour-market pressures that drove rapid, unusual policy shifts.",
  "Louisiana__2008"            = "Louisiana shifted to cluster 2 in 2008 as continued Katrina-driven demographic change — large inflows of Latino construction workers — forced the state's immigration policy mix to diverge from its historical Southern-cluster neighbours.",

  # ── SOUTHERN ENFORCEMENT WAVE 2008 ────────────────────────────────────────
  "South Carolina__2008"       = "South Carolina jumped 21 ranks in 2008, joining Tennessee and North Carolina in a Southern enforcement wave. All three states adopted broad 287(g) agreements and began moving toward omnibus-style legislation.",
  "Tennessee__2008"            = "Tennessee's distance surged in 2008, propelled by one of the largest own-cluster spikes in the dataset. Rapid growth of the state's immigrant population prompted a suite of enforcement measures — 287(g) agreements, E-Verify bills, and limits on public benefits — that set it apart from both clusters.",
  "North Carolina__2008"       = "North Carolina moved sharply away from the centroid in 2008 as the state expanded 287(g) jail agreements across multiple counties. Combined with rapid Latino population growth in the Research Triangle and Charlotte, the state's policy profile became increasingly distinctive.",

  # ── RECESSION & POLICY RETRENCHMENT 2009–2011 ─────────────────────────────
  "Nebraska__2011"             = "Nebraska dropped 17 ranks in 2011 — moving closer to the centroid — as the Great Recession constrained its enforcement ambitions. The state scaled back several 287(g) plans and deferred omnibus legislation, converging back toward the national norm.",
  "Wisconsin__2011"            = "Wisconsin moved sharply closer to the centroid in 2011 after its cluster shifted from 2 to 1 in the prior year. Budget pressures during the recession curtailed enforcement expenditure, and the state's profile re-aligned with other Midwestern states.",
  "Alabama__2011"              = "Alabama enacted HB56 in 2011 — one of the strictest omnibus immigration laws in the country, modelled on Arizona's SB1070. This pushed Alabama's distance to its highest recorded level, 14 ranks above its prior-year position.",

  # ── CALIFORNIA / WEST COAST DIVERGENCE ────────────────────────────────────
  "California__2011"           = "California began its sustained upward divergence in 2011, eventually reaching the highest extremity score in the entire dataset. AB131 (2011) extended in-state tuition and financial aid to undocumented students, while the state simultaneously expanded drivers-license access and limited cooperation with ICE detainers.",
  "California__2013"           = "California's extremity score peaked near 7.6 in 2013 — the highest value in the dataset — as the AB60 drivers-license bill passed and the Trust Act (limiting ICE detainer compliance) took effect, putting California further from any other state than any observation in the data.",
  "California__2016"           = "California reached its highest global distance rank (furthest from the centroid) in 2016 after SB1001 banned mandatory E-Verify for private employers and SB54 (the sanctuary bill) moved through the legislature, cementing its position as the most distinctive state in the dataset.",

  # ── COLORADO & VIRGINIA: GRADUAL INTEGRATION SHIFT ────────────────────────
  "Colorado__2014"             = "Colorado switched from cluster 1 to cluster 2 permanently in 2014, reflecting a sustained shift toward integration-oriented policies including drivers licenses for undocumented residents (2013) and expanded in-state tuition — a trajectory that moved it away from its historical neighbours.",
  "Virginia__2014"             = "Virginia oscillated between clusters 2011–2016 as Northern Virginia's rapidly growing immigrant population created political tension between pro-enforcement rural legislators and more integration-oriented suburban representatives.",

  # ── WEST VIRGINIA ANOMALY ─────────────────────────────────────────────────
  "West Virginia__2015"        = "West Virginia's cluster switch in 2015 is notable given how sparse its immigrant population is. The movement reflects adoption of several public-benefits restrictions (no Medicaid for LPR adults or unauthorised adults) that suddenly aligned its profile with higher-immigrant enforcement-oriented states.",

  # ── ILLINOIS LATE DIVERGENCE ──────────────────────────────────────────────
  "Illinois__2017"             = "Illinois began a sharp upward divergence in 2017 after the TRUST Act and expanded healthcare for undocumented children pushed its profile toward California's integrationist cluster, producing the second-highest extremity score in the dataset by 2019.",

  # ── MONTANA ANOMALY ───────────────────────────────────────────────────────
  "Montana__2019"              = "Montana jumped 17 ranks in 2019 — the largest single-year rank change in the final period — despite having one of the smallest immigrant populations in the country. The shift reflects adoption of several enforcement-adjacent measures (E-Verify expansion, English-only provisions) that moved a historically neutral state sharply away from the centroid."
)

extract_series <- function(prefix, years) {
  cols    <- paste0(prefix, "_", years)
  present <- cols[cols %in% names(df)]
  yrs_out <- years[paste0(prefix, "_", years) %in% names(df)]
  mat     <- df[, present, drop = FALSE]
  list(years  = as.integer(yrs_out),
       states = df$state,
       values = lapply(seq_len(nrow(mat)), function(i) as.numeric(mat[i, ])))
}

extract_clusters <- function(years) {
  cols    <- paste0("cluster_", years)
  present <- cols[cols %in% names(df)]
  yrs_out <- years[paste0("cluster_", years) %in% names(df)]
  mat     <- df[, present, drop = FALSE]
  list(years  = as.integer(yrs_out),
       states = df$state,
       values = lapply(seq_len(nrow(mat)), function(i) as.integer(mat[i, ])))
}

payload <- list(
  global_raw          = extract_series("dist_1cs",             YEARS),
  global_raw_delta    = extract_series("dist_1cs_delta",       DELTA_YEARS),
  global_ranked       = extract_series("rank_1cs",             YEARS),
  global_ranked_delta = extract_series("rank_1cs_delta",       DELTA_YEARS),
  own_raw             = extract_series("dist_own",             YEARS),
  own_raw_delta       = extract_series("dist_own_delta",       DELTA_YEARS),
  own_ranked          = extract_series("rank_own",             YEARS),
  ext_raw             = extract_series("extremity",            YEARS),
  ext_raw_delta       = extract_series("extremity_delta",      DELTA_YEARS),
  ext_ranked          = extract_series("rank_extremity",       YEARS),
  ext_ranked_delta    = extract_series("rank_extremity_delta", DELTA_YEARS),
  clusters            = extract_clusters(YEARS),
  all_dist_1cs        = extract_series("dist_1cs",             YEARS),
  all_rank_1cs        = extract_series("rank_1cs",             YEARS),
  all_delta_1cs       = extract_series("dist_1cs_delta",       DELTA_YEARS),
  all_dist_own        = extract_series("dist_own",             YEARS),
  all_rank_own        = extract_series("rank_own",             YEARS),
  all_delta_own       = extract_series("dist_own_delta",       DELTA_YEARS),
  all_extremity       = extract_series("extremity",            YEARS),
  all_rank_ext        = extract_series("rank_extremity",       YEARS),
  all_delta_ext       = extract_series("extremity_delta",      DELTA_YEARS),
  annotations         = annotations
)

json_payload <- toJSON(payload, auto_unbox = TRUE, digits = 6, na = "null")
```

```{=html}
<link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css">
<link rel="stylesheet" href="https://cdn.datatables.net/fixedcolumns/4.3.0/css/fixedColumns.dataTables.min.css">
<style>
  body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif; }

  /* ── CONTROLS ── */
  .ctrl-block  { margin-bottom: 14px; }
  .ctrl-label  { font-size: 11px; text-transform: uppercase; letter-spacing:.07em; color:#888; margin-bottom:5px; font-weight:700; }  /* EDIT */
  .ctrl-row    { display:flex; gap:6px; flex-wrap:wrap; }
  .ctrl-btn    { padding:5px 14px; border:1px solid #ccc; border-radius:6px; background:#fff; color:#444;
                 cursor:pointer; font-size:12.5px; font-weight:500; line-height:1.4;
                 transition:background .12s,color .12s,border-color .12s; }  /* EDIT */
  .ctrl-btn:hover   { background:#f0f5fa; border-color:#aac4db; }
  .ctrl-btn.active  { background:#2E86B5; color:#fff; border-color:#2E86B5; }  /* EDIT: active color */
  .ctrl-btn.disabled{ opacity:.35; cursor:not-allowed; pointer-events:none; }
  .ctrl-subtitle    { font-size:12px; color:#666; margin:5px 0 0 0; line-height:1.5; min-height:1.3em; font-style:italic; }  /* EDIT */

  /* ── TABLE OUTER BORDER ── */
  #tbl-outer   { border:1px solid #ccc; border-radius:8px; overflow:hidden; margin-top:16px; }
  #tbl-wrapper { overflow-x:auto; }

  /* ── DATATABLE ── */
  table.display { border-collapse:collapse; font-size:12px; width:100% !important; }
  table.display thead th {
    background:#f7f7f7; border-bottom:2px solid #ddd;
    /* EDIT: increase padding here if sort arrows still overlap */
    padding:8px 20px 8px 8px !important;
    white-space:nowrap; font-weight:600; color:#333;
    text-align:center !important; position:relative;
  }
  table.display thead th:first-child  { text-align:left !important; padding-left:10px !important; }
  table.display thead th:nth-child(2) { text-align:left !important; }
  /* Push DataTables sort arrows to far right so they never overlap text */
  table.display thead th.sorting::after,
  table.display thead th.sorting_asc::after,
  table.display thead th.sorting_desc::after {
    position:absolute; right:4px; top:50%; transform:translateY(-50%);
  }
  table.display tbody td {
    padding:0 !important; border-bottom:1px solid #f0f0f0;
    text-align:right; white-space:nowrap; vertical-align:middle;
  }
  table.display tbody td:first-child  { text-align:left; font-weight:500; }
  table.display tbody td:nth-child(2) { text-align:left; }
  table.display tbody tr:hover td     { background:rgba(46,134,181,.06) !important; }
  table.display tbody tr:last-child td{ border-bottom:none; }
  /* Remove the gap between thead and first tbody row */
  table.display tbody tr:first-child td { border-top: none; }

  /* ── CELL INNER DIV — uniform height, centred content ── */
  .cv {
    display:flex; align-items:center; justify-content:center;
    padding:4px 10px;
    /* EDIT: row height */
    min-height:36px; box-sizing:border-box;
  }
  table.display tbody td:first-child .cv { justify-content:flex-start; padding:4px 14px 4px 10px; }
  table.display tbody td:nth-child(2) .cv { justify-content:flex-start; padding:4px 6px; }

  /* ── SPARKLINE ── */
  .spark-cell { display:flex; align-items:center; padding:2px 6px; min-height:36px; box-sizing:border-box; }

  /* ── BUBBLE ── */
  .bubble { border-radius:999px; padding:2px 8px; font-weight:700; display:inline-block; }

  /* ── ANNOTATION UNDERLINE ── */
  .has-note { border-bottom:1px dotted rgba(0,0,0,0.35); cursor:help; }

  .dtfc-fixed-left { background:#fff; }

  /* ── TOOLTIP ── */
  #cell-tooltip {
    position:fixed; z-index:9999; background:#fff; border:1px solid #ccc; border-radius:7px;
    padding:9px 13px; font-size:12px; color:#333; box-shadow:0 4px 16px rgba(0,0,0,.12);
    pointer-events:none; display:none; max-width:300px; line-height:1.7;
  }
  #cell-tooltip .tt-title  { font-weight:700; font-size:13px; margin-bottom:4px; color:#222; }  /* EDIT */
  #cell-tooltip .tt-row    { display:flex; justify-content:space-between; gap:12px; }
  #cell-tooltip .tt-label  { color:#777; }
  #cell-tooltip .tt-note   { margin-top:6px; padding-top:6px; border-top:1px solid #eee; color:#444; font-style:italic; font-size:11.5px; }  /* EDIT */
</style>
```

```{r inject-data, results='asis'}
cat(sprintf('<script>const RAW_DATA = %s;</script>\n', json_payload))
```

```{=html}
<!-- ── CONTROLS ── -->
<div id="controls">
  <div class="ctrl-block">
    <div class="ctrl-label">Measure of Distance</div>  <!-- EDIT -->
    <div class="ctrl-row" id="row-measure">
      <button class="ctrl-btn active" data-val="global"    onclick="setMeasure(this)">Proximity to Global Centroid</button>   <!-- EDIT -->
      <button class="ctrl-btn"        data-val="own"       onclick="setMeasure(this)">Proximity to Cluster Centroid</button>  <!-- EDIT -->
      <button class="ctrl-btn"        data-val="extremity" onclick="setMeasure(this)">Extremity</button>                      <!-- EDIT -->
    </div>
    <div class="ctrl-subtitle" id="sub-measure"></div>
  </div>
  <div class="ctrl-block">
    <div class="ctrl-label">Variable Transformation</div>  <!-- EDIT -->
    <div class="ctrl-row" id="row-transform">
      <button class="ctrl-btn active" data-val="raw"          onclick="setTransform(this)">Raw</button>            <!-- EDIT -->
      <button class="ctrl-btn"        data-val="raw_delta"    onclick="setTransform(this)">Raw Deltas</button>     <!-- EDIT -->
      <button class="ctrl-btn"        data-val="ranked"       onclick="setTransform(this)">Ranked</button>         <!-- EDIT -->
      <button class="ctrl-btn"        data-val="ranked_delta" onclick="setTransform(this)">Ranked Deltas</button>  <!-- EDIT -->
    </div>
    <div class="ctrl-subtitle" id="sub-transform"></div>
  </div>
  <div class="ctrl-block">
    <div class="ctrl-label">Color Scheme</div>  <!-- EDIT -->
    <div class="ctrl-row" id="row-color">
      <button class="ctrl-btn active" data-val="none"          onclick="setColor(this)">No Color</button>                   <!-- EDIT -->
      <button class="ctrl-btn"        data-val="clusters"      onclick="setColor(this)">Colored Clusters</button>           <!-- EDIT -->
      <button class="ctrl-btn"        data-val="extremes"      onclick="setColor(this)">Extremes</button>                   <!-- EDIT -->
      <button class="ctrl-btn"        data-val="top1"          onclick="setColor(this)">Top 1</button>                      <!-- EDIT -->
      <button class="ctrl-btn"        data-val="clusters_top1" onclick="setColor(this)">Colored Clusters + Top 1</button>   <!-- EDIT -->
      <button class="ctrl-btn"        data-val="extremes_top1" onclick="setColor(this)">Extremes + Top 1</button>           <!-- EDIT -->
    </div>
    <div class="ctrl-subtitle" id="sub-color"></div>
  </div>
</div>

<div id="tbl-outer">
  <div id="tbl-wrapper">
    <table id="main-dt" class="display"></table>
  </div>
</div>
<div id="cell-tooltip"></div>

<script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/fixedcolumns/4.3.0/js/dataTables.fixedColumns.min.js"></script>

<script>
// ═══════════════════════════════════════════════════════════════════════════
// COLOUR CONSTANTS  — EDIT here
// ═══════════════════════════════════════════════════════════════════════════
const CLUSTER_COLORS = { "1": "#B5463A", "2": "#2E86B5" };
const EXTREMES_CLOSE = "#009E73";   // EDIT
const EXTREMES_FAR   = "#D55E00";   // EDIT
const N_TOP          = 3;           // EDIT: number of extremes highlighted

// ═══════════════════════════════════════════════════════════════════════════
// SUBTITLE STRINGS  — EDIT any string
// ═══════════════════════════════════════════════════════════════════════════
const SUB_MEASURE = {
  global:    "Euclidean distance of each state from the overall centroid across all states and years.",
  own:       "Euclidean distance of each state from the centroid of its assigned cluster.",
  extremity: "How far each state sits from the centre of the full distribution — a measure of uniqueness."
};
const SUB_TRANSFORM = {
  raw:          "Raw distance values for each year (2000–2019).",
  raw_delta:    "Year-over-year change in raw distance (2001–2019). Positive = moving away; negative = moving closer.",
  ranked:       "States ranked by distance each year (2000–2019). Lower rank = closer to centroid.",
  ranked_delta: "Year-over-year change in rank (2001–2019). Negative = improved (moved closer); positive = worsened."
};
const SUB_COLOR = {
  global: {
    none:"No highlighting applied.",
    clusters:"Cell fill shows each state's cluster membership.",
    extremes:"The 3 closest (green) and 3 furthest (orange) states each year are highlighted. Ties are all colored.",
    top1:"The closest state(s) each year carry a bubble in that state's cluster color.",
    clusters_top1:"Cluster fill applied; the closest state(s) carry a white bubble.",
    extremes_top1:"Extremes highlighted; the closest state(s) carry a white bubble."
  },
  own: {
    none:"No highlighting applied.",
    clusters:"Cell fill shows each state's cluster membership.",
    extremes:"The 3 closest states per cluster each year are filled using that cluster's color. Ties are all colored.",
    top1:"The closest state(s) per cluster carry a bubble in that cluster's color.",
    clusters_top1:"Cluster fill applied; the closest state(s) per cluster carry a white bubble.",
    extremes_top1:"Closest 3 per cluster highlighted; the closest per cluster carry a white bubble."
  },
  extremity: {
    none:"No highlighting applied.",
    clusters:"Cell fill shows each state's cluster membership.",
    extremes:"The 3 most extreme (furthest) states each year are highlighted in orange. Ties are all colored.",
    top1:"The most extreme state(s) each year carry a bubble in orange.",
    clusters_top1:"Cluster fill applied; the most extreme state(s) carry a white bubble.",
    extremes_top1:"Top 3 most extreme highlighted; the most extreme state(s) carry a white bubble."
  }
};

// ═══════════════════════════════════════════════════════════════════════════
// STATE
// ═══════════════════════════════════════════════════════════════════════════
let curMeasure="global", curTransform="raw", curColor="none", dtInstance=null;

// ═══════════════════════════════════════════════════════════════════════════
// COLOUR HELPERS
// ═══════════════════════════════════════════════════════════════════════════
function hexToRgb(h){return[parseInt(h.slice(1,3),16)/255,parseInt(h.slice(3,5),16)/255,parseInt(h.slice(5,7),16)/255];}
function rgbToHex(r,g,b){return'#'+[r,g,b].map(v=>Math.round(Math.min(1,Math.max(0,v))*255).toString(16).padStart(2,'0')).join('');}
function lighten(hex,amt){const[r,g,b]=hexToRgb(hex);return rgbToHex(r+amt*(1-r),g+amt*(1-g),b+amt*(1-b));}

// ═══════════════════════════════════════════════════════════════════════════
// INDEX HELPERS  — all return sets, all handle ties
// ═══════════════════════════════════════════════════════════════════════════
function nSmallestSet(arr,n){
  const sorted=arr.map((v,i)=>({v,i})).filter(x=>x.v!==null&&!isNaN(x.v)).sort((a,b)=>a.v-b.v);
  if(!sorted.length)return new Set();
  const cutoff=sorted[Math.min(n,sorted.length)-1].v;
  return new Set(sorted.filter(x=>x.v<=cutoff).map(x=>x.i));
}
function nLargestSet(arr,n){
  const sorted=arr.map((v,i)=>({v,i})).filter(x=>x.v!==null&&!isNaN(x.v)).sort((a,b)=>b.v-a.v);
  if(!sorted.length)return new Set();
  const cutoff=sorted[Math.min(n,sorted.length)-1].v;
  return new Set(sorted.filter(x=>x.v>=cutoff).map(x=>x.i));
}
function nSmallestAbsSet(arr,n){
  const sorted=arr.map((v,i)=>({v:Math.abs(v),i})).filter(x=>!isNaN(x.v)).sort((a,b)=>a.v-b.v);
  if(!sorted.length)return new Set();
  const cutoff=sorted[Math.min(n,sorted.length)-1].v;
  return new Set(sorted.filter(x=>x.v<=cutoff).map(x=>x.i));
}
function nLargestAbsSet(arr,n){
  const sorted=arr.map((v,i)=>({v:Math.abs(v),i})).filter(x=>!isNaN(x.v)).sort((a,b)=>b.v-a.v);
  if(!sorted.length)return new Set();
  const cutoff=sorted[Math.min(n,sorted.length)-1].v;
  return new Set(sorted.filter(x=>x.v>=cutoff).map(x=>x.i));
}
// Top-1: all tied for best value
function top1Set(arr,isDelta,measure){
  const valid=arr.filter(v=>v!==null&&!isNaN(v));
  if(!valid.length)return new Set();
  if(isDelta||measure==='own'){
    const mn=Math.min(...valid.map(Math.abs));
    return new Set(arr.map((v,i)=>({v,i})).filter(x=>Math.abs(x.v)===mn).map(x=>x.i));
  }
  if(measure==='extremity'){
    const mx=Math.max(...valid);
    return new Set(arr.map((v,i)=>({v,i})).filter(x=>x.v===mx).map(x=>x.i));
  }
  const mn=Math.min(...valid);
  return new Set(arr.map((v,i)=>({v,i})).filter(x=>x.v===mn).map(x=>x.i));
}
function top1PerCluster(arr,clustVals,isDelta){
  const result=new Set();
  const clusters=[...new Set(clustVals.filter(c=>c!==null))];
  clusters.forEach(cl=>{
    const inCl=arr.map((v,i)=>({v,i})).filter(x=>clustVals[x.i]==cl&&x.v!==null&&!isNaN(x.v));
    if(!inCl.length)return;
    const mn=Math.min(...inCl.map(x=>x.v));
    inCl.filter(x=>x.v===mn).forEach(x=>result.add(x.i));
  });
  return result;
}

// ═══════════════════════════════════════════════════════════════════════════
// HIGHLIGHTS
// ═══════════════════════════════════════════════════════════════════════════
function computeHighlights(colVals,clustVals,measure,isDelta,colorMode){
  const hl=new Map();
  let closeIdx=new Set(), farIdx=new Set(), top1Idx=new Set();

  if(measure==='own'){
    // Extremes: N closest per cluster (no far side)
    const clusters=[...new Set(clustVals.filter(c=>c!==null))];
    clusters.forEach(cl=>{
      const inCl=colVals.map((v,i)=>({v,i})).filter(x=>clustVals[x.i]==cl&&x.v!==null&&!isNaN(x.v));
      const sorted=inCl.sort((a,b)=>a.v-b.v);
      if(!sorted.length)return;
      const cutoff=sorted[Math.min(N_TOP,sorted.length)-1].v;
      sorted.filter(x=>x.v<=cutoff).forEach(x=>closeIdx.add(x.i));
    });
    top1Idx=top1PerCluster(colVals,clustVals,isDelta);

  }else if(measure==='extremity'){
    if(!isDelta){
      farIdx   =nLargestSet(colVals,N_TOP);
      top1Idx  =top1Set(colVals,false,'extremity');
    }else{
      closeIdx =nSmallestAbsSet(colVals,N_TOP);
      top1Idx  =top1Set(colVals,true,'extremity');
    }
  }else{
    // global
    if(!isDelta){
      closeIdx=nSmallestSet(colVals,N_TOP);
      farIdx  =nLargestSet(colVals,N_TOP);
      top1Idx =top1Set(colVals,false,'global');
    }else{
      closeIdx=nSmallestAbsSet(colVals,N_TOP);
      farIdx  =nLargestAbsSet(colVals,N_TOP);
      top1Idx =top1Set(colVals,true,'global');
    }
  }

  colVals.forEach((_,ri)=>{
    const cl=clustVals?String(clustVals[ri]):null;
    let cellBg=null, bubbleBg=null;

    // Cell fill
    if(colorMode==='clusters'||colorMode==='clusters_top1'){
      if(cl&&CLUSTER_COLORS[cl]) cellBg=CLUSTER_COLORS[cl];
    }
    if(colorMode==='extremes'||colorMode==='extremes_top1'){
      if(measure==='own'){
        if(closeIdx.has(ri)&&cl&&CLUSTER_COLORS[cl]) cellBg=CLUSTER_COLORS[cl];
      }else if(measure==='extremity'){
        if(farIdx.has(ri))   cellBg=EXTREMES_FAR;
        if(closeIdx.has(ri)) cellBg=EXTREMES_CLOSE;
      }else{
        if(closeIdx.has(ri)) cellBg=EXTREMES_CLOSE;
        if(farIdx.has(ri))   cellBg=EXTREMES_FAR;
      }
    }

    // Bubble
    const isTop1=top1Idx.has(ri)&&(colorMode==='top1'||colorMode==='clusters_top1'||colorMode==='extremes_top1');
    if(isTop1){
      if(colorMode==='clusters_top1'||colorMode==='extremes_top1'){
        // White bubble — cell fill already carries the information
        bubbleBg='rgba(255,255,255,0.88)';
      }else{
        // Top 1 only: color by measure
        if(measure==='own'){
          // Use cluster color
          bubbleBg=(cl&&CLUSTER_COLORS[cl])?lighten(CLUSTER_COLORS[cl],0.5):'#eeeeee';
        }else if(measure==='extremity'){
          // Single color: orange (furthest)
          bubbleBg=lighten(EXTREMES_FAR,0.5);
        }else{
          // global: single color: green (closest)
          bubbleBg=lighten(EXTREMES_CLOSE,0.5);
        }
      }
    }

    if(cellBg||bubbleBg) hl.set(ri,{cellBg,bubbleBg});
  });
  return hl;
}

// ═══════════════════════════════════════════════════════════════════════════
// SPARKLINE
// ═══════════════════════════════════════════════════════════════════════════
function buildSparkline(vals,gMin,gMax){
  const W=88,H=28,P=3;
  const rng=gMax-gMin||1;
  const pts=vals.map((v,i)=>{
    if(v===null||isNaN(v))return null;
    const x=P+(i/Math.max(vals.length-1,1))*(W-P*2);
    const y=H-P-((v-gMin)/rng)*(H-P*2);
    return `${x.toFixed(1)},${y.toFixed(1)}`;
  }).filter(Boolean);
  if(pts.length<2)return`<svg width="${W}" height="${H}"></svg>`;
  return`<svg width="${W}" height="${H}" style="display:block;overflow:visible"><polyline points="${pts.join(' ')}" fill="none" stroke="#2E86B5" stroke-width="1.5" stroke-linejoin="round" stroke-linecap="round"/></svg>`;  /* EDIT: stroke color */
}

// ═══════════════════════════════════════════════════════════════════════════
// SORT
// ═══════════════════════════════════════════════════════════════════════════
function rowAvg(arr){const v=arr.filter(x=>x!==null&&!isNaN(x));return v.length?v.reduce((a,b)=>a+b,0)/v.length:Infinity;}
function rowAvgAbs(arr){const v=arr.filter(x=>x!==null&&!isNaN(x));return v.length?v.reduce((a,b)=>a+Math.abs(b),0)/v.length:Infinity;}

// ═══════════════════════════════════════════════════════════════════════════
// TOOLTIP
// ═══════════════════════════════════════════════════════════════════════════
function ordinal(n){
  if(n===null||isNaN(n))return'—';
  // Use the raw value (which may be fractional for ties, e.g. 4.5)
  if(!Number.isInteger(n)) return n.toFixed(1);
  const s=Math.round(n);
  const sfx=['th','st','nd','rd'];
  const v=s%100;
  return s+(sfx[(v-20)%10]||sfx[v]||sfx[0]);
}
function sgn(v){return v>=0?'+':'';}
function getVal(slice,si,yr){
  const yi=slice.years.indexOf(yr);
  if(yi<0||si>=slice.values.length)return null;
  return slice.values[si][yi];
}

function buildTooltip(state,year){
  const yr=parseInt(year);
  const si=RAW_DATA.global_raw.states.indexOf(state);
  if(si<0)return'';
  const cyi=RAW_DATA.clusters.years.indexOf(yr);
  const cluster=(cyi>=0&&si<RAW_DATA.clusters.values.length)?RAW_DATA.clusters.values[si][cyi]:null;
  let lines=[];

  if(curMeasure==='global'){
    const raw  =getVal(RAW_DATA.all_dist_1cs, si,yr);
    const rank =getVal(RAW_DATA.all_rank_1cs, si,yr);
    const delta=getVal(RAW_DATA.all_delta_1cs,si,yr);
    if(raw  !==null) lines.push(['Distance', raw.toFixed(4)]);
    // Show rank exactly as it appears in the data (may be 4.5 for ties)
    if(rank !==null) lines.push(['Rank', `${ordinal(rank)} of 50`]);
    if(delta!==null) lines.push(['Δ prior year', `${sgn(delta)}${delta.toFixed(4)}`]);
  }else if(curMeasure==='own'){
    const raw  =getVal(RAW_DATA.all_dist_own, si,yr);
    const rank =getVal(RAW_DATA.all_rank_own, si,yr);
    const delta=getVal(RAW_DATA.all_delta_own,si,yr);
    if(raw  !==null) lines.push(['Cluster distance', raw.toFixed(4)]);
    if(rank !==null) lines.push(['Within-cluster rank', `${ordinal(rank)}`]);
    if(delta!==null) lines.push(['Δ prior year', `${sgn(delta)}${delta.toFixed(4)}`]);
  }else{
    const raw  =getVal(RAW_DATA.all_extremity,si,yr);
    const rank =getVal(RAW_DATA.all_rank_ext, si,yr);
    const delta=getVal(RAW_DATA.all_delta_ext,si,yr);
    if(raw  !==null) lines.push(['Extremity', raw.toFixed(4)]);
    if(rank !==null) lines.push(['Rank', `${ordinal(rank)} of 50`]);
    if(delta!==null) lines.push(['Δ prior year', `${sgn(delta)}${delta.toFixed(4)}`]);
  }
  if(cluster!==null) lines.push(['Cluster', String(cluster)]);

  const annNote=RAW_DATA.annotations&&RAW_DATA.annotations[`${state}__${yr}`];
  // EDIT: tooltip title format — currently "State, Year"
  let html=`<div class="tt-title">${state}, ${yr}</div>`;
  html+=lines.map(([l,v])=>`<div class="tt-row"><span class="tt-label">${l}</span><span>${v}</span></div>`).join('');
  if(annNote) html+=`<div class="tt-note">&#128204; ${annNote}</div>`;
  return html;
}

// ═══════════════════════════════════════════════════════════════════════════
// BUILD TABLE
// ═══════════════════════════════════════════════════════════════════════════
function buildTableData(){
  const key=curMeasure==='extremity'?'ext_'+curTransform:curMeasure+'_'+curTransform;
  const slice=RAW_DATA[key];
  if(!slice)return null;
  const {states,years,values:vals}=slice;
  const clustDat=RAW_DATA.clusters;
  const isDelta=curTransform.includes('delta');
  const isRanked=curTransform.includes('ranked');

  // Shared sparkline scale
  let gMin=Infinity,gMax=-Infinity;
  vals.forEach(row=>row.forEach(v=>{if(v!==null&&!isNaN(v)){if(v<gMin)gMin=v;if(v>gMax)gMax=v;}}));

  // Cluster year map
  const cym={};
  clustDat.years.forEach((yr,ci)=>{cym[yr]=ci;});

  // Per-column highlights
  const colHL=years.map((yr,yi)=>{
    const cv=vals.map(r=>r[yi]);
    const ci=cym[yr];
    const clv=ci!==undefined?clustDat.values.map(r=>r[ci]):new Array(states.length).fill(null);
    return computeHighlights(cv,clv,curMeasure,isDelta,curColor);
  });

  // Sort order
  const avgs=vals.map(row=>(curMeasure==='extremity'&&!isDelta)?-rowAvg(row):isDelta?rowAvgAbs(row):rowAvg(row));
  const order=avgs.map((v,i)=>({v,i})).sort((a,b)=>a.v-b.v).map(x=>x.i);

  const rows=order.map(si=>{
    const state=states[si];
    const row=[
      `<div class="cv">${state}</div>`,
      `<div class="spark-cell">${buildSparkline(vals[si],gMin,gMax)}</div>`
    ];
    years.forEach((yr,yi)=>{
      const raw=vals[si][yi];
      const h=colHL[yi].get(si);
      const hasNote=RAW_DATA.annotations&&RAW_DATA.annotations[`${state}__${yr}`];
      const disp=(raw===null||raw===undefined||isNaN(raw))?''
        :isRanked?(Number.isInteger(raw)?String(raw):raw.toFixed(1))
        :raw.toFixed(4);

      const da=`data-state="${state}" data-year="${yr}"`;
      const noteClass=hasNote?' has-note':'';

      if(!h){
        row.push(`<div class="cv${noteClass}" ${da}>${disp}</div>`);
      }else{
        const bg=h.cellBg?`background:${h.cellBg};`:'';
        const fg=h.cellBg?'color:#fff;':'';
        if(h.bubbleBg){
          row.push(`<div class="cv${noteClass}" style="${bg}${fg}" ${da}><span class="bubble" style="background:${h.bubbleBg};color:#222;">${disp}</span></div>`);
        }else{
          row.push(`<div class="cv${noteClass}" style="${bg}${fg}" ${da}>${disp}</div>`);
        }
      }
    });
    return row;
  });
  return{years,rows};
}

// ═══════════════════════════════════════════════════════════════════════════
// RENDER
// ═══════════════════════════════════════════════════════════════════════════
function render(){
  document.getElementById('sub-measure').textContent   =SUB_MEASURE[curMeasure]||'';
  document.getElementById('sub-transform').textContent =SUB_TRANSFORM[curTransform]||'';
  document.getElementById('sub-color').textContent     =(SUB_COLOR[curMeasure]||{})[curColor]||'';

  const result=buildTableData();
  if(!result){
    document.getElementById('tbl-wrapper').innerHTML='<p style="color:#c00;padding:1rem;">Not available for this combination.</p>';
    return;
  }
  const{years,rows}=result;
  const columns=[
    {title:'State',orderable:true},   /* EDIT: column header */
    {title:'Trend',orderable:false}   /* EDIT: column header */
  ].concat(years.map(y=>({title:String(y),orderable:true})));

  if(dtInstance){
    dtInstance.destroy();
    document.getElementById('tbl-wrapper').innerHTML='<table id="main-dt" class="display"></table>';
  }
  dtInstance=$('#main-dt').DataTable({
    data:rows, columns, paging:false, searching:false, info:false,
    ordering:true, order:[], scrollX:true,
    fixedColumns:{leftColumns:2},
    columnDefs:[
      {targets:0,width:'130px',className:'dt-left',orderable:true},
      {targets:1,width:'100px',className:'dt-left',orderable:false},
      {targets:'_all',className:'dt-center',width:'70px'}
    ],
    dom:'t',
    createdRow:function(row){$(row).find('td').css('padding','0');}
  });
  attachTooltips();
}

// ═══════════════════════════════════════════════════════════════════════════
// TOOLTIP EVENTS
// ═══════════════════════════════════════════════════════════════════════════
function attachTooltips(){
  const tip=document.getElementById('cell-tooltip');
  $(document).off('mouseenter mousemove mouseleave','.cv[data-state]');
  $(document).on('mouseenter','.cv[data-state]',function(e){
    const html=buildTooltip(this.dataset.state,this.dataset.year);
    if(!html)return;
    tip.innerHTML=html; tip.style.display='block'; posTip(e);
  });
  $(document).on('mousemove','.cv[data-state]',function(e){posTip(e);});
  $(document).on('mouseleave','.cv[data-state]',function(){tip.style.display='none';});
}
function posTip(e){
  const tip=document.getElementById('cell-tooltip');
  const tw=tip.offsetWidth||260,th=tip.offsetHeight||120;
  let x=e.clientX+14,y=e.clientY+14;
  if(x+tw>window.innerWidth -10) x=e.clientX-tw-10;
  if(y+th>window.innerHeight-10) y=e.clientY-th-10;
  tip.style.left=x+'px'; tip.style.top=y+'px';
}

// ═══════════════════════════════════════════════════════════════════════════
// BUTTON HANDLERS
// ═══════════════════════════════════════════════════════════════════════════
function setMeasure(btn){
  curMeasure=btn.dataset.val;
  document.querySelectorAll('#row-measure .ctrl-btn').forEach(b=>b.classList.toggle('active',b===btn));
  enforceConstraints(); render();
}
function setTransform(btn){
  if(btn.classList.contains('disabled'))return;
  curTransform=btn.dataset.val;
  document.querySelectorAll('#row-transform .ctrl-btn').forEach(b=>b.classList.toggle('active',b===btn));
  render();
}
function setColor(btn){
  curColor=btn.dataset.val;
  document.querySelectorAll('#row-color .ctrl-btn').forEach(b=>b.classList.toggle('active',b===btn));
  render();
}
function enforceConstraints(){
  const rd=document.querySelector('[data-val="ranked_delta"]');
  if(curMeasure==='own'){
    rd.classList.add('disabled');
    if(curTransform==='ranked_delta'){
      curTransform='raw_delta';
      document.querySelectorAll('#row-transform .ctrl-btn').forEach(b=>b.classList.toggle('active',b.dataset.val===curTransform));
    }
  }else rd.classList.remove('disabled');
}

// ═══════════════════════════════════════════════════════════════════════════
// INIT
// ═══════════════════════════════════════════════════════════════════════════
$(document).ready(function(){enforceConstraints();render();});
</script>
```
